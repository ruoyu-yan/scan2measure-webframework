diff --git a/src/LineDetection3D.cpp b/src/LineDetection3D.cpp
index 5481644..0dc98b7 100644
--- a/src/LineDetection3D.cpp
+++ b/src/LineDetection3D.cpp
@@ -563,7 +563,7 @@ void LineDetection3D::lineFromMask( cv::Mat &mask, int thLineLengthPixel, std::v
 	
 	std::vector<std::vector<cv::Point> > contours;  
 	std::vector<cv::Vec4i> hierarchy;  
-	cv::findContours(mask2, contours, hierarchy, CV_RETR_LIST, CV_CHAIN_APPROX_NONE);
+	cv::findContours(mask2, contours, hierarchy, cv::RETR_LIST, cv::CHAIN_APPROX_NONE);
 
 	// B. line fitting from the contours
 	for ( int i=0; i<contours.size(); ++i )
diff --git a/src/main.cpp b/src/main.cpp
index 6c26f15..40abd4b 100644
--- a/src/main.cpp
+++ b/src/main.cpp
@@ -154,10 +154,15 @@ void writeOutLinesObj(string filePath,
   file.close();
 }
 
-int main() 
+int main( int argc, char** argv )
 {
-	string fileData = "D://Facade//data.txt";
-	string fileOut  = "D://Facade//data";
+	if ( argc < 3 )
+	{
+		cout << "Usage: LineFromPointCloud <input.txt> <output_prefix>" << endl;
+		return 1;
+	}
+	string fileData = argv[1];
+	string fileOut  = argv[2];
 
 	// read in data
 	PointCloud<double> pointData; 
